<li>
	{{burger_name}}

	<button class="devoured" data-id="{{id}}" data-devoured="{{tobedevoured}}">
		{{#if tobedevoured}}Devour It !{{else}}FULL!{{/if}}
	</button>

	<button class="delete_burger" data-id="{{this.id}}">DELETE!</button>
</li>
