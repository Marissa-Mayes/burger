<h1>Burgers</h1>

<h2>Time to eat the burgers</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burger-block tobedevoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burger menu!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burger-block tobedevoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add custome burger</h2>
<form class="create-form">
  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="bu" name="name">
  </div>

  <div class="form-group">
    <label for="userChoice">Burger time</label><br>
    <input type="radio" name="burger" value="1" new burger><br>
    <input type="radio" name="sleepy" value="0"> 
  </div>

  <button class="newBurgerButton">add burger</button>
</form>