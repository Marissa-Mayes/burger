<h1>Eat-Da-Burger!</h1>
<div class= "center">
<img src="../assets/images/burger.jpg" width="250px" height="200px"></div>

<h2>Time to eat the burgers</h2>
<div class= "burger_list">
  <div class= "container">

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burger-block tobedevoured=true}}
    {{/unless}}
  {{/each}}
</ul>
  </div>

<h2>Burger menu!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burger-block tobedevoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h3>Add custome burger</h3>
<form class="create-form">
  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="bu" name="name">
  </div>
  <button class="newBurgerButton">add burger</button>
</form>